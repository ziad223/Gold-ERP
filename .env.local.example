# Frontend (Next.js) — local development. Copy to .env.local
# The frontend talks to the backend ONLY through this URL.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_API_ORIGIN=http://localhost:8000

# Data source: "api" (real backend) | "mock" | "local"
NEXT_PUBLIC_DATA_SOURCE=api
