# DARFUS Client C2C3R — Generated `next-env.d.ts` Policy

بالعربي المختصر: `next-env.d.ts` ملف مولّد من Next.js، ولا يجوز تعديله أو إرجاعه يدويًا. يقبل النظام فقط نسختَي Next.js المدعومتين الموثقتين أدناه.

## Authority

- Control: `DARFUS-CLIENT-C2C3R-NEXT-ENV-RUNTIME-PARITY-RESOLUTION-01`
- File: `next-env.d.ts`
- Owner rule: generated runtime artifact; never manually edit, revert, or auto-repair it.
- Project guardrail: `AGENTS.md` forbids running `next dev` during acceptance.

## Supported variants

| Runtime/toolchain context | Supported generated import | Policy |
|---|---|---|
| Next development toolchain | `./.next/dev/types/routes.d.ts` | Accepted only when generated by the supported dev toolchain; not manually edited |
| Next production build toolchain | `./.next/types/routes.d.ts` | Accepted only when generated by `npm run build`; not manually edited |

The complete generated file must retain the standard Next.js reference lines and the generated-file note. A transition is accepted only when it is a one-line import transition between the two exact supported imports. Any other path, added content, or unrelated diff is unsupported and must stop for Owner review.

## Current observation

After the controlled `npm run build` run in this control, the current file contains:

```ts
import "./.next/types/routes.d.ts";
```

The file was not edited by this control. The earlier C2C3 pre-build observation of the dev import is retained as supporting evidence only; a fresh dev regeneration was not run because the project guardrail forbids `next dev` during acceptance.

## Guard implementation

The narrow policy implementation is:

- `scripts/next-env-runtime-policy.cjs`
- `tests/c2c3r-next-env-runtime-policy.test.cjs`

The implementation validates the full template, exact supported variant, explicit manual-edit rejection, and both supported one-line transition directions. It does not modify `next-env.d.ts`.

## Operational rule

1. Run the permitted toolchain command.
2. Read the generated file and classify it against the command context.
3. Confirm the served runtime was produced from that build.
4. Never repair the file by hand.
5. Stop on any unsupported content or variant.

`next-env.d.ts` is therefore not a product source-of-truth file and is not a reason to revert a valid generated transition between the two supported variants.
