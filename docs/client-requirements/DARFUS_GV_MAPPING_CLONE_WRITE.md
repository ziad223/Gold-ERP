# Gift Voucher Mapping — Clone Write

The minimum clone write created only two `SystemAccountRole` rows for `GIFT_VOUCHER_LIABILITY`, one for each active branch, both pointing to existing account `ACC-5554f72e-7edd-484d-8db7-b2be4764aac2` / code `2400`.

No account, branch, Treasury mapping, Tax setting, Voucher, Invoice, Payment, Journal, or Inventory row was created by the mapping step.

`CLONE_MAPPING_WRITE = MINIMUM_ONLY`.
`CLONE_DUPLICATE_ACTIVE_ROLE_COUNT = 0`.

