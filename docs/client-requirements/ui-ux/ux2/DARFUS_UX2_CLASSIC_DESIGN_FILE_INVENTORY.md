# UX-2 Classic Design File Inventory

The pre-change inventory contains 48 production frontend/design files. It covers global CSS, theme/config, locale root and dashboard shell, shared layout/UI, AR/EN messages, i18n helpers, representative POS/Inventory/Accounting/Gold/Settings surfaces and print/tag presentation. No `.env`, credentials, `node_modules` or `.next` content was copied.

The complete path list, category, size and SHA-256 are in `DARFUS_UX2_CLASSIC_DESIGN_HASH_MANIFEST.md` and the physical snapshot manifest.

`CLASSIC_DESIGN_FILE_INVENTORY = COMPLETE`
