# UX-2 Typography Hooks

Foundation hooks distinguish `--font-brand`, `--font-operational`, `--font-numeric`, `--font-arabic` and `--font-english` without broad page migration. Brand and operational typefaces remain separate; numeric presentation remains tabular and LTR-isolated where required.

`UX2_TYPOGRAPHY_HOOKS = PASS`
