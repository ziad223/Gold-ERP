# UX-2 Reduced Motion Foundation

Global CSS includes `@media (prefers-reduced-motion: reduce)` with near-zero transition/animation duration, disabled scrolling motion and no critical workflow dependency on animation.

`UX2_REDUCED_MOTION_FOUNDATION = PASS`
