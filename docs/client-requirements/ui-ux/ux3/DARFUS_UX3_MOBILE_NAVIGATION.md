# UX-3 Mobile Navigation

At 390×844, the localized menu trigger opens the existing sidebar drawer. The drawer exposes the same permission-filtered links, the navigation landmark is visible, and the localized close control closes the drawer. The active route remains marked with `aria-current="page"`.

No new mobile route, account action, permission check, or business action was introduced.
