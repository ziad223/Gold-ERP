# UX-3 Dark / Light

Before and after paired Dashboard captures were retained in the pre-UX3 and UX3 snapshot directories. The existing theme toggle remains the theme authority. UX-3 styles consume UX-2 semantic aliases (`--surface-1`, `--surface-selected`, `--text-*`, `--gold-primary`, `--focus`) and do not add a second token system.

`DARK_LIGHT_SHELL = PASS`

`THEME_AUTHORITY_CHANGED = NO`
