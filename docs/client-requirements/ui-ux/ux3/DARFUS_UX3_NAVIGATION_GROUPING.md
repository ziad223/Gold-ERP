# UX-3 Navigation Grouping

The existing visual groups are preserved: Overview, Sales & customers, Assets & inventory, Finance management, and System. UX-3 does not remove, merge, rename, or relocate a business route. Group labels remain localized through the existing `Navigation` catalog.

`NAV_VISIBILITY_PRESENTATION != PERMISSION_AUTHORITY`

`NAV_GROUPING_CHANGED = PRESENTATION_HOOKS_ONLY`

`ROUTE_CONTRACT_CHANGED = NO`
