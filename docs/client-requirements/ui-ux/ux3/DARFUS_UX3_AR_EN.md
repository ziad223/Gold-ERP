# UX-3 Arabic / English

The browser matrix covered AR and EN for Dashboard, POS, Inventory, Accounting, Gold Center, and Settings. The root element reported `lang=ar, dir=rtl` for Arabic and `lang=en, dir=ltr` for English. Shell labels are sourced from the existing navigation translations; breadcrumb labels are explicitly paired in the new shell component.

`AR_SHELL = PASS`

`EN_SHELL = PASS`

`RTL_LTR_SHELL = PASS`
