# UX5C — Disabled Checkout Clarity

The native `disabled` expression is unchanged. The button now has an explicit
neutral disabled surface, readable muted text, border, and not-allowed cursor in
light and dark themes. No enablement condition or `completeSale` handler changed.

`CHECKOUT_DISABLED_VISUAL_CLARITY = PASS`
`CHECKOUT_ENABLEMENT_LOGIC_CHANGED = NO`
