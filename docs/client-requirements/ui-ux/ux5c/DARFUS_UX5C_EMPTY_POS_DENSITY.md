# UX5C — Empty POS Density

The empty invoice panel remains structurally bounded but uses a smaller
`min-h-[170px]` empty state with balanced padding. No invoice item is fabricated,
and the cart, pricing, payment, and checkout state remain unchanged.

`EMPTY_POS_DENSITY = PASS`
