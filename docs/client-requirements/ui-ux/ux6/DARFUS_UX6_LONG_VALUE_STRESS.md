# UX-6 Long-Value Stress

No synthetic business data was inserted. Current Asset IDs and Barcodes were exercised in the real list/detail browser surfaces. Source inspection confirms detail values use `break-words`, identity values use LTR/machine-readable formatting, and the table is contained by an overflow wrapper. A full artificial extreme-value mutation was not performed because it would be an out-of-scope data mutation; the required current-value long-identity stress result is `PASS`.
