# UX-6 Change Map

| File | Change boundary |
|---|---|
| `app/[locale]/(dashboard)/inventory/page.tsx` | Summary strip, spacing/density, accessible search label, readable EN status labels, table semantics/alignment; existing data and handlers unchanged |
| `app/[locale]/(dashboard)/inventory/[id]/page.tsx` | Field typography/wrapping, localized lifecycle status display, readable detail hierarchy; existing action handlers unchanged |
| `tests/ux6-inventory-assets-presentation.test.cjs` | Focused static presentation/authority guard tests only |

Forbidden and unchanged: backend, API, database, schema, migrations, config, permissions, routes, business logic, POS, Gift Voucher, GBW, supplier receive, accounting, and master data.

