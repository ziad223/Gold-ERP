# UX5B Mobile Evidence

EN Light and AR Dark at 420px viewport were captured. Cards stack, payment buttons remain usable in two columns, totals and checkout remain visible, long text wraps, and the page has no horizontal overflow (`bodyScrollWidth = 405`, `bodyClientWidth = 405`). The dense table uses its existing bounded horizontal table container rather than expanding the page.

`MOBILE_POPULATED_POS = PASS`.
