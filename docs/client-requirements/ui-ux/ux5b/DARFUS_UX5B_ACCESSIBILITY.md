# UX5B Accessibility Evidence

The fixture exposes labelled inputs, a named payment group, `aria-pressed` selected state, native buttons/inputs, disabled state, remove controls, and a visible focusable control structure. Status is represented by text (`AVAILABLE`), not color alone. RTL/LTR and touch-sized controls were checked in the responsive states.

`POPULATED_POS_ACCESSIBILITY = PASS`.
