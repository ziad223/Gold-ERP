# UX-12 Invoice and Print

Invoice Search & Print rendered populated records and Print controls in Chrome. Prior UX-11C direct print-media/fixed-format evidence remains valid because no preview/print source changed. `npm run test:print-export` could not launch its configured Playwright headless executable (`headless_shell.exe` missing); this is an environment evidence blocker, not a product result. No print or reprint mutation was executed.
