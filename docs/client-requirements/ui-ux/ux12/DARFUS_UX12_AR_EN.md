# UX-12 Arabic / English

AR routes rendered with `lang=ar`, `dir=rtl`; EN routes rendered with `lang=en`, `dir=ltr`. Representative populated surfaces were checked in both locales. The repaired reset control uses the existing localized `resetLabel`, proven as `إعادة ضبط` in AR and `Reset` in EN.

No technical/internal UX wording was introduced into user-facing surfaces.
