# Table Pattern Research

Use task-appropriate tables: responsive line items for moderate data, desktop-centric grids only where large-set analysis needs them, and list/detail on small screens. Preserve the primary identity column, use priority columns and responsive details, align financial numerics, keep filtering/sorting/action menus clear, and make sticky headers/columns non-overlapping. No table behavior changed.
