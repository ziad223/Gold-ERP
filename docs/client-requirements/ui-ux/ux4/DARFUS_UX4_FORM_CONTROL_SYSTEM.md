# UX4 Form Control System

`Input`, `Textarea`, `Checkbox`, `Radio`, and `Switch` are standalone shared presentation primitives. Native checkbox/radio semantics and native events are preserved. `Switch` exposes `role=switch`, `aria-checked`, disabled state, and a controlled callback. No field requiredness, payload, validation, or business workflow changed.

Result: `FORM_CONTROL_SYSTEM = PASS` by focused contract/accessibility tests.
