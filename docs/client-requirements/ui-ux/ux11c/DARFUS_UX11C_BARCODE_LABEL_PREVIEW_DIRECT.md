# BarcodeLabelPreview Direct Proof

The disposable route mounted the actual `BarcodeLabelPreview` with synthetic value `UX11C-SYNTHETIC-000001`. AR and EN showed the label, value, readable text, and the real `ScannableBarcode` renderer. The barcode renderer was visible at 310.42×28px; Light and Dark checks showed no inversion, clipping, or transform blur. The print button was not clicked and it remained disabled without print permission.

`BARCODE_LABEL_PREVIEW_DIRECT = PASS`; `BARCODE_LABEL_PREVIEW_TABLET = PASS`; `BARCODE_HIGH_CONTRAST = PASS`.
