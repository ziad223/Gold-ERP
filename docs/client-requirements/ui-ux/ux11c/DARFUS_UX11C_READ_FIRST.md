# UX-11C Read First

The complete UX-11C control was read before execution. It authorizes only a disposable evidence harness, with the main worktree and `darfus_erp` frozen. It requires direct ReceiptPreview and BarcodeLabelPreview proof, the existing print-export fixture and test intent, 840x1180 Chrome evidence, AR/EN, Light/Dark, print media, zero mutation, disposable cleanup, and main-source parity. UX-12 and all open Gift Voucher/CGP tracks remain untouched.

The Owner master working-method file was searched for and was not present; it is recorded as missing rather than inferred.
