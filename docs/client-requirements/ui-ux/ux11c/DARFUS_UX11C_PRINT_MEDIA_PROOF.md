# UX-11C Print Media Proof

The local Chrome runner called `page.emulateMedia({ media: 'print' })`. `media=true`, fixture root count was 1, template counts were Luxury 5 / Compact 3 / Minimal 3 / Thermal 3, overflow was false, and screen-only elements were hidden as expected. No print dialog or print mutation was triggered.

`PRINT_MEDIA_DIRECT_BROWSER_PROOF = PASS`.
