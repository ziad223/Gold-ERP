# UX-11C Light / Dark

ReceiptPreview and BarcodeLabelPreview were directly rendered in both shell class states. Their fixed-format surfaces remained readable; the barcode renderer stayed visible and the synthetic barcode value remained unchanged. The print fixture was also rendered under both shell states; print media remained deterministic. `UX11C_LIGHT = PASS`; `UX11C_DARK = PASS`.
