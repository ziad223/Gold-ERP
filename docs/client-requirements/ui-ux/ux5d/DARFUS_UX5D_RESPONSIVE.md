# UX-5D Responsive Evidence

Desktop, tablet and mobile screenshots were captured in AR/EN. The amount summary uses `repeat(auto-fit,minmax(10rem,1fr))`, preserving readable cards without a permanent internal scroll. Buttons remain visible and the panel wraps within the available width.

Result: `RESPONSIVE = PASS`.

