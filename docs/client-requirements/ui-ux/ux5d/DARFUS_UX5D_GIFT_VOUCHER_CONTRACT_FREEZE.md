# UX-5D Gift Voucher Contract Freeze

The public component contract is frozen. The following props and event meanings were not changed: `rtl`, `code`, `voucher`, `loading`, `error`, `supported`, `currency`, `formatAmount`, `remainingDue`, `onCodeChange`, `onValidate`, `onRemove`.

No handler body, state transition, API caller, calculation, payment compatibility rule, or checkout behavior was changed. Existing Enter validation and Remove behavior remain intact.

