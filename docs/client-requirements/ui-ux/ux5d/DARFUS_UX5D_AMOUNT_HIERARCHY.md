# UX-5D Amount Hierarchy

Face value, applied amount, and remaining due remain the same existing values and labels. They are now separated into readable cards with an adaptive grid. No arithmetic, VAT, payment, or currency rule was added; all values still use the existing `formatAmount` inputs.

Result: `AMOUNT_HIERARCHY = PASS`; `CLIENT_RECALCULATION = NO`.

