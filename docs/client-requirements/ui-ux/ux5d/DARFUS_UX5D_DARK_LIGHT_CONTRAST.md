# UX-5D Dark/Light Contrast

The voucher panel, active-state card, warning, error, labels, values, borders and focus ring use existing semantic utility tokens with scoped light/dark variants. Browser evidence covers EN/AR light and dark desktop states. No color communicates a new business state.

Result: `LIGHT = PASS`, `DARK = PASS`, `CONTRAST = PASS`.

