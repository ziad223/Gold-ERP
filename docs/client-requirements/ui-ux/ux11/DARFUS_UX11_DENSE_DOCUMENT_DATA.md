# UX-11 Dense Document Data

Invoice and receipt templates retain their existing dense tables, labels, totals, payment fields, and view-model inputs. UX11 does not hide, reorder, recalculate, or rename any data. Long identifiers use the existing template behavior; the scoped preview stylesheet adds safe wrapping for code/pre/machine-value containers only.

