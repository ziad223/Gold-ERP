# UX-11 After Hash Manifest

After snapshot: `backups/ui-ux/UX11_PRINT_PREVIEW_20260828T223310Z/`

The complete post-change SHA-256 list is in `after-source.sha256.txt`; copied source is under `source/`; browser evidence is under `screenshots/`; rollback rehearsal hashes are under `rollback/ROLLBACK_REHEARSAL_HASHES.txt`.

Intentional UX11 product-source changes are limited to the scoped preview CSS imports/classes and shared print CSS isolation. The new focused test and UX11 documentation are batch artifacts. Existing worktree dirtiness remains separately attributable.

