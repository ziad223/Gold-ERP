# UX-11 Accessibility

The scoped preview stylesheet adds `:focus-visible` outlines for interactive descendants, preserves existing semantic buttons/labels, supports keyboard-visible focus, and does not rely on hover. Reduced-motion media rules disable prolonged transitions/animations in the preview surface. Touch remains native because no pointer-only interaction was introduced.

