# UX-9 Dense Financial Data

The scoped stylesheet preserves table semantics, applies tabular numeric figures, keeps identifiers readable with `unicode-bidi: plaintext`, and bounds dense tables to their local scroll frame on narrow screens. It does not alter values, ordering, pagination, filters, or source projections.
