# UX-6B Barcode / Tag Regression

- Barcode parity and Asset identity regression selection: PASS.
- Tag Profile parity regression selection: PASS.
- Existing barcode/tag contract keeps stored value `GWBGL22000001`, CODE128 renderer, tag data mapper, and print identity unchanged.
- No scanner acceptance is claimed; visual readability only was proven.

`BARCODE_REGRESSION = PASS`; `TAG_PROFILE_REGRESSION = PASS`; `ASSET_IDENTITY_REGRESSION = PASS`.

