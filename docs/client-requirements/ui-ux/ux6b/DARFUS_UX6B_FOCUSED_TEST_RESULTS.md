# UX-6B Focused Tests

`node --test tests/ux6b-asset-tag-preview-theme.test.cjs` → 4 passed, 0 failed.

The focused guard proves explicit face/container theme isolation, retained canonical tag/barcode data path, retained print permission/action/renderer, and absence of mutation/API/DB paths in the changed component.

