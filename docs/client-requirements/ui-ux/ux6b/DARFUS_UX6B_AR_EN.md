# UX-6B AR / EN

After browser evidence:

- EN: LTR shell, readable tag text and barcode, English print button.
- AR: RTL shell, readable tag text and barcode; barcode rendering remains LTR and is not reversed; Arabic print button remains reachable.

The same stored Asset ID and Barcode were observed in both locales. `UX6B_AR = PASS`, `UX6B_EN = PASS`, `BARCODE_DIRECTION_CHANGED_BY_RTL = NO`.

