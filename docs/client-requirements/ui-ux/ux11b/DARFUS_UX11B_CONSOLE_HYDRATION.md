# Console / Hydration

Reachable main-runtime browser evidence returned no application console errors or warnings and no hydration/page errors. The print-export runner reported only the expected 404 resource error from the unavailable fixture, with `pageErrors = []`. This environment error is not treated as a product hydration defect.

`CONSOLE_APPLICATION_ERRORS = 0`; `HYDRATION_ERRORS = 0` for reachable surfaces.
