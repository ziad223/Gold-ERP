# UX-11B Light / Dark

The reachable ClientAssetTagPreview was inspected after toggling the existing theme control. The fixed tag face stayed white with dark readable text in both Light and Dark shell modes; the original shell theme was restored. No business or print action was invoked. The invoice dialog was reachable in both locale paths, but ReceiptPreview and BarcodeLabelPreview direct theme proof is blocked by missing runtime mounts.

`UX11B_LIGHT = PARTIAL`; `UX11B_DARK = PARTIAL`; overall gate remains blocked for required direct component coverage.
