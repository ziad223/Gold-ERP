# InvoicePrintOptionsDialog

Canonical route: `/en/sales/search-print` → read-only invoice detail → existing `Print invoice` options dialog. The dialog opened and exposed Document Type, Template, Language, Cancel, and Print. At 840x1180, body/document overflow was absent. A native select received keyboard focus with a computed solid focus outline. The final Print action was not clicked; `INVOICE_PRINT_ACTION_TRIGGERED = NO`.

AR/EN and shell theme evidence was collected where reachable. Preview prerequisites did not become ready in this read-only instance, so no preview-root claim is made for the dialog.
