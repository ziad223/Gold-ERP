# UX-1R Compact Production Shell

The approved `Obsidian Atelier` identity is retained while the editorial UX-1 hero is compressed into an operational shell:

`DARFUS / Reference workspace` → eyebrow → page title → prototype tabs → business surface.

Oversized hero spacing and the large Reference Surface treatment were reduced. Brand serif remains a brand moment; operational content uses readable sans typography. No production shell or navigation was changed.

Evidence: `app/test/ux1-reference/page.tsx`, `ux1-reference.module.css`, browser matrix, and the served `/en/test/ux1-reference` route.

`COMPACT_PRODUCTION_SHELL = PASS`
