# UX-1R Accessibility Recheck

Real-browser baseline passed for the isolated surface:

- named locale/theme controls and text inputs;
- tab roles with `aria-selected`;
- visible focus outline on the Customer input;
- disabled checkout distinction;
- visible `role=alert` and `aria-live` loading state;
- labelled finance table region and table headers;
- RTL/LTR root direction and touch-sized controls.

The prototype remains static/read-only. No WCAG certification is claimed.

`ACCESSIBILITY_REFINEMENT = PASS_BASELINE`
