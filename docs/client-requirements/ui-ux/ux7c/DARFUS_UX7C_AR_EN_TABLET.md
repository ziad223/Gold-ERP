# UX7C AR/EN Tablet Evidence

The direct runtime was launched with Arabic locale and measured `840×1180`; the login/dashboard DOM was RTL. EN Customer/Supplier Tablet evidence was not accepted because the authenticated Branch context was unavailable before the required surfaces could be reached.

`UX7C_AR_TABLET = BLOCKED_AUTH_BRANCH_CONTEXT`

`UX7C_EN_TABLET = BLOCKED_AUTH_BRANCH_CONTEXT`
