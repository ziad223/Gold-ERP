# Contact / Identifier Readability

PASS. Phone, email and IDs keep their source values. `.ux7-contact-value` and `.ux7-identifier` use bidi-safe rendering and `overflow-wrap: break-word` so RTL/LTR screens do not reorder or clip business identifiers.
