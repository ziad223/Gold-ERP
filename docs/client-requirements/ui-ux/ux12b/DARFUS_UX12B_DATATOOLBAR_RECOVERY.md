# UX-12B DataToolbar Recovery Recheck

At 390×844 the existing localized reset names were present: AR `إعادة ضبط` on Customers, Accounting and Audit; EN `Reset` on Customers, Accounting and Audit. The accepted one-line repair remains exactly `aria-label={resetLabel}`. The native button, `onReset`, disabled predicate, filter state and permissions were not changed. No additional source change was applied.
