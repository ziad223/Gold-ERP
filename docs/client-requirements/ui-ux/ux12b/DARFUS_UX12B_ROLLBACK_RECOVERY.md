# UX-12B Rollback Recovery

The prior accepted DataToolbar repair was rehearsed in an isolated copy: pre-image `5DAB…` was reproduced after removing only the attribute, then current image `A6D5…` was reproduced by reapplying it. The real worktree was never replaced. `UX12B_ROLLBACK_REHEARSAL = PASS`; no rollback was applied.
