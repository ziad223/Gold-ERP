# POS Pattern Research

The current POS hierarchy is Customer → Search → Invoice Items → Payment → Totals. Future visual work should preserve that sequence while improving scan speed: prominent search, clear Asset/barcode identity, weight/making visibility where already supplied, customer context, payment hierarchy, large total/remaining due, keyboard/touch reachability, low-latency feedback, and immediate error recovery. No checkout or payment behavior changed.
